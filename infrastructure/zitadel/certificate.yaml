apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zitadel-tls
  namespace: zitadel
spec:
  secretName: zitadel-tls-secret
  dnsNames:
    - auth.192.168.1.249.sslip.io
  issuerRef:
    name: sslip-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
